export enum PublicRoutes {
  HOME = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  NOT_FOUND = "404",
  DATASET = "/dataset",
  GROUPS = "/groups",
  ORGANIZATIONS = "/organizations",
  ABOUT = "/about",
}

export enum PrivateRoutes {}
